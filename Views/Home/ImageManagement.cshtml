@*@model IEnumerable<PetSitter.Models.ImageStore>

@{
    ViewData["Title"] = "ListSavedImages";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="display-4">Image Management</h2>
@Html.ValidationSummary(false, string.Empty, new { @class = "text-danger" })

<form asp-controller="Home" asp-action="ImageManagement"
      method="post" enctype="multipart/form-data">
    <div class="row">
        <div class="col-sm-3 col-md-2">
            <img src="~/Image/ImageUpload.png" alt="Images in a folder"
                 style="width: 100px;" />
        </div>
        <div class="col-sm-8">
            <div class="form-group">
                <label for="fileInput">Select an image to upload to the server:</label>
                <input type="file" name="fileInput" class="form-control-file" />
            </div>
            <button type="submit" class="btn btn-primary" style="margin-top:10px;">
                Upload
            </button>
        </div>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ImageId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FileName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ImageId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FileName)
                </td>
                <td>
                    <img style="max-width: 400px;" src="data:image/png;base64,
                        @Convert.ToBase64String(item.Image)" alt="@item.FileName" />
                </td>
            </tr>
        }

        @if (ViewData["SuccessMessage"] != null)
        {
            <div class="alert alert-success" role="alert">
                @ViewData["SuccessMessage"]
            </div>
        }
    </tbody>
</table>
*@